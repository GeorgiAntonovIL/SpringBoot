server:
  port : 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/JpaLab?createDatabaseIfNotExist=true
    username: postgres
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        show-sql: false
        format-sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
#  h2:
#    console:
#      enabled: false